# Postgres connection string used by Prisma.
DATABASE_URL="postgresql://user:password@localhost:5432/kajianmap?schema=public"

# Long, random secret used to sign/verify JWTs (server/helpers/jwt.ts).
# Generate one with: openssl rand -base64 48
JWT_SECRET="replace-with-a-long-random-secret"
